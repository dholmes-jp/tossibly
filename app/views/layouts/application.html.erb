<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Tossibly" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Tossibly">
    <meta name="mobile-web-app-capable" content="yes">
    <%= tag.link rel: "manifest", href: pwa_manifest_path %>
    <%= render 'shared/ios_pwa_links' %>
    <meta name="turbo-cache-control" content="no-preview">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <!-- Open Graph / start -->
    <meta property="og:title" content="Tossibly">
    <meta property="og:description" content="Tossibly helps you find the easiest way to give away, recycle, or dispose of unwanted items.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:image" content="<%= image_url("og-image.png") %>">
    <!-- Open Graph / end -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div id="page-curtain" class="page-curtain" data-turbo-permanent></div>
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    <%= yield %>
  </body>
</html>
